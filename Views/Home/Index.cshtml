@model HomeViewModel

<h1>Лучшие автомобили</h1>
<div class="row mt-5 mb-5 col ml-2 mr-2">
    @{
        foreach (Car car in Model.FavCars)
        {
            @Html.Partial("AllCars", car)
        }
    }
</div>